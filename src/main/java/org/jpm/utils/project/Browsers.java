package org.jpm.utils.project;

public enum Browsers {

    /*
    Not in use currently but we can force the input we want to take as browser names,
     similarly for other params as well from config file if we wnat ot force inputs
    */

    FIREFOX,
    CHROME,
    SAFARI
}
